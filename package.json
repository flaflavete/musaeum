{
  "name": "musaeum-tts-proxy",
  "version": "1.0.0",
  "description": "Proxy TTS para o Musæum — esconde a chave ElevenLabs do frontend",
  "type": "module",
  "main": "server.cjs",
  "scripts": {
    "start": "node server.cjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
